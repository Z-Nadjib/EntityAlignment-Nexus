# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.ipynb_checkpoints/

# --- Virtual environments ---
.venv/
venv/
env/

# --- Datasets (large, not redistributed in the repo) ---
Data/
logs/

# --- Run artefacts -----------------------------------------------------------
# experiments/ IS tracked: the configs, metrics, losses and curves of every run
# are the evidence behind the reported numbers (~2 MB in total).
# The weights are not: checkpoints and embedding tables are ~1.8 GB and are
# regenerated by re-running the config.
*.pt
*.pth

# --- Built documentation ---
site/

# --- Editor / tooling / OS ---
.claude/
.vscode/
.idea/
.DS_Store
Thumbs.db
